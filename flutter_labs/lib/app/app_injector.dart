class AppInjector {
  static Future<void> init() async {
    // TODO: Initialize shared preferences, firebase, and other services
    // Example:
    // final sharedPrefs = await SharedPreferences.getInstance();
    // GetIt.I.registerSingleton<SharedPreferences>(sharedPrefs);
  }
}
